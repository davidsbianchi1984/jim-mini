# status: backlog — 13 rows
# ceiling: 13
#
# iOS fields the Swift shape guard saw a route not return, which are real.
#
# `test_the_shape_the_swift_client_expects.py` drives every GET binding in
# `native/ios/Sources/ApiClient.swift` and checks each struct against what the
# route actually sends — the names, and whether the declared type can decode
# the shape that arrives. QRME's copy found nine fictions in its own Swift
# client on the first run, every one a defect already fixed on the Windows
# side and never carried across.
#
# This client had none. All twenty-two of these are the same shape of answer:
# a route that returns a short *nothing yet* body until the account has a
# history, and its full shape afterwards.
#
# The 0.56.5 rule is that driving into a state beats recording that you did
# not, and where that was two calls — arming the crash watch, building an
# adaptation profile — the guard makes them. These cannot be manufactured:
# continuity is derived from accumulated check-ins, coach turns and
# helpfulness answers over time, and there is no route that builds one. A
# fixture that faked the history would be asserting against its own fiction.
#
# The care-team and mic rows joined when 0.61.1+ typed the phones. The
# care-team route answers `{"linked": false}` and nothing else until the
# household links its QRME organization, and linking is validated across the
# tandem — no fixture inside this suite can stand up a real org. `note` is
# sent by attach, handover and release — the state GET this guard drives
# never carries it, and the struct is shared.
#
# Ratcheted: this list may shrink freely and grows only by raising the
# ceiling in the same commit — a deliberate, visible edit. A row leaves it
# when somebody writes a test that lives the state and reads the field.
#
# ## after 1.8.9 — seventeen rows lived, one learned
#
# This file said continuity "cannot be manufactured: there is no route
# that builds one." There is no single route — and three check-ins plus
# two answered guidance rounds build one anyway. The fixture lives that
# state now, which verified the six continuity fields, the check-in
# trend, and the helpfulness tally for real. The presence area and
# boundary rows were never dead at all: they sit inside `[String: X]`
# maps, and the walker was comparing their fields against the map's own
# keys — condition names, area names — so fields genuinely on the wire
# read as fiction (the C# walker skipped maps entirely; both fixed, with
# canaries). `ContinuityState.note` joins going the other way: it is the
# "nothing yet" sentence, and a fixture that builds the history stops
# seeing it — the same shape as AdaptationProfile.note.
#
AdaptationProfile.note        # only while `built` is false — the sentence
                              # saying there is no profile yet
ContinuityState.note          # the "nothing yet" sentence — only until the
                              # history builds, and the fixture now builds one
BaselineMetric.state          # once a personal baseline has bands to be in
PresenceSpoken.due            # once a beat is scheduled
PresenceSpoken.slot_from_hour
PresenceBeat.deepened_line    # only when a real model answers — under the stub
                              # `deepen` returns the offline line and says so
CareTeamState.org_id          # only once linked, and linking needs a real
CareTeamState.department_id   # QRME organization through the tandem
CareTeamState.credential_held
CareTeamState.latest_plan
MicStateRow.note              # attach/handover/release send it; the state GET
                              # never does, and the struct is shared
MedRow.taken_today            # only on as-needed rows; the fixture's first
MedRow.max_per_day            # row is scheduled, and the checker reads the
                              # first row of the list
