# status: backlog — 3 rows
#
# English strings held by both the desktop console's table and a shell's, on
# which no console row and no native row share a wording. Checked by
# test_the_desktop_and_the_phone_say_different_things.py, matched exactly in
# both directions.
#
# ## What this measured here
#
#     ios       61 English strings in both tables, 25 with no agreed wording
#     android   65                                 27
#     windows   62                                 27
#
# Nine more were counted at first and were not real. This repo's console table
# writes some rows escaped — `"破棄する"`, which in TypeScript
# *is* 破棄する and renders correctly. Comparing source bytes calls that a
# disagreement, and the guard would have shipped demanding it be fixed.
#
#     asked     are these two source strings identical
#     mattered  do these two strings render the same
#
# ## What it was hiding
#
# The alarm and safety surface — the set this audit keeps naming as the case
# where English is a hazard rather than a discourtesy. Here the two clients
# were not both English. They were two different sentences in the reader's own
# language:
#
#   * *JIM demande : ça va ?* on the desktop against *JIM demande : est-ce que
#     ça va ?* on the phone — the question the crash watch asks;
#   * **Cancel** as *Kündigen* on the desktop, which is the German for
#     terminating a contract, against *Abbrechen* on the phone;
#   * **Medical ID** as *Identité médicale* against *Fiche médicale*, on the
#     card a responder reads;
#   * **Disarm** as *Desativar* against *Desarmar*;
#   * *Armed — {name} will be contacted…* as *kontaktiert* against
#     *benachrichtigt*.
#
# All reconciled onto the phones' wording.
#
# ## What is left
#
# Two example values and one unit label.
#
# **0.48.2 struck the fourth.** *Translate* was recorded here as a row on a
# third axis: `action.translate` on the iPhone against `ov.translate.go` on the
# other two shells, disagreeing with each other, so no single native wording
# existed for the console to adopt. That was true, and it is now fixed rather
# than recorded — the three shells were measured against each other for the
# first time, agreed on 翻訳する and अनुवाद करें in two places out of three, and
# the odd one out moved. `Me` and `When something breaks` came with it.
#
# The third axis is closed: `test_the_three_shells_say_the_same_thing.py`,
# zero rows in all three products.
#
# ceiling: 3
android: rch.acc.handle.ph :: care.handle.ph  ("@you" — an example handle)
android: bas.cw.name.ph :: res.name.ph  ("Rosa" — an example name; one table localises it and the other does not)
windows: bas.cw.window :: cw.minutes  ("Minutes per attempt")
