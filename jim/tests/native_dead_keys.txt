# status: floor — 0 rows
#
# Rows in a shell's L10n table that nothing in that shell asks for. Checked by
# test_a_shell_asks_for_a_key_it_has.py, and ratcheted in two directions: no
# shell may hold more than `ceiling`, and the shells together may not hold more
# than `total`.
#
# **Empty, as of 0.40.7.** The four that stood here were generic action verbs —
# `action.refresh`, `.save`, `.send`, `.translate` — added in advance because a
# screen would obviously need a Save button, translated into ten languages, and
# asked for by no screen in any shell across several releases. This file's own
# instruction was "wire one or delete one"; they were deleted. A screen that
# needs one adds the row it needs, in the wording it needs.
#
# The two-direction ratchet is new here too. `ceiling` is a maximum over the
# three shells, so the number of dead rows could have risen — iOS three to
# four, a fourth shell arriving with four of its own — while the check passed
# every time, because no single shell crossed the line.
#
#     asked     is any one shell's dead-key count above the line
#     mattered  is the number of dead rows going up
#
# ceiling: 4
# total: 0
