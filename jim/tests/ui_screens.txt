# status: floor — 34 rows
#
# Which console surface each screen drawing depicts.
#
# The gallery tests next door all check screens against the README: that a
# referenced screen exists, that an existing screen is shown, that the
# numbering has no gap. Every one of them starts from the screens. None asks
# whether a *surface* has a screen at all — so a feature can ship with nothing
# drawn and no test says a word. That has now happened three times: voice
# cloning, the recoverable watermark and the chat role went two whole versions
# undrawn, and the error-reporting card and its first-run notice went out in
# 0.19.0 the same way.
#
# This file is the missing direction. Every component under app/src/screens/
# and every top-level surface component must appear here, so a new one fails
# the suite until somebody decides what it looks like.
#
#   <component>   <screens>   one or more numbers, comma-separated
#   <component>   undrawn     acknowledged as having no drawing
#   <component>   unaudited   seeded, never verified — see below
#
# `undrawn` is a ratchet: the count may fall and must never rise. Adding a
# surface without a drawing therefore fails, which is the whole point.
#
# ceiling: undrawn=0 unaudited=0
#
# `unaudited` was the honest seed — components that predated the file, their
# screens never confirmed one by one. 0.42.3 audited the last six by reading
# each component's own heading against the gallery's titles: four were only
# unlabelled (`Meds` draws 85, `PaceCue` is the pace circle of 14, the
# sign-in flow of `Onboarding` is 40 and 42, `ProviderTiles` is the tile
# picker of 83 — not 20, which is the *human* providers screen), and two had
# never been drawn at all. `Safety`, the answering end of the crash watch,
# had no drawing while screen 88 showed only the watch asking; `Wellness`
# shipped three whole generators with nothing in the gallery. 102 and 103
# are those drawings, and `undrawn=0` is true for the first time since the
# column was seeded. `unaudited` is not a status a new component may use.

Baseline        8,13
CareTeam        18
Channel         12
Checkin         5
Coach           4,7,14
Community       21
Feed            33,34
Presence        35,36
Home            1
Journal         6,19
Monitor         3
Settings        2

Meds            17
Onboarding      9,10
Safety          31
Wellness        32
PaceCue         4
ProviderTiles   15

Problems        22
ProblemNotice   23

# The six that closed the console backlog. Each is one family of the 109
# routes the desktop app could not reach.
Aims            24
Wards           25
Held            26
Attending       27
Reach           28
Bearing         29
SelfProfile     30
Access          37

Engaged         38
Talk            39
Studio          40

Watch           41   # the shell; its 36 faces are docs/watch's own gallery
Hands           42
Capabilities    43
