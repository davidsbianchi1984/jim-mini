# status: backlog — 0 rows
# ceiling: 0
#
# One wire field name carrying more than one type across this product's API.
#
# `spoken` was the row that made this file. It meant three things at once:
# the CPR playbook steps read aloud (a string list), the model's rewritten
# presence line (a string), and whether a beat was said out loud (a boolean).
# The Windows client had already forked it into three records without anybody
# noticing what the forking meant, and TypeScript could not fork it — one
# interface `extends` another there — so `tsc` had been failing on `main` and
# nothing in any suite ran `tsc`.
#
# Two kinds of row, and the first is the sharper one:
#
#   * a **primitive** clash (bool/int, bool/string, double/int, string/list)
#     is dangerous, because a decoder coerces rather than refusing and the
#     reader gets a plausible wrong answer;
#   * an **object-shape** clash (`Row[]` vs `int`, one record vs another) is
#     usually a count beside a collection, or two views of one concept. A
#     decoder fails loudly on those, so somebody finds out.
#
# Neither is good. A field name is the one piece of an API a reader carries
# from one route to the next, and a name that changes meaning between them is
# the reader being misled by the thing they were told to rely on.
#
# Read from the Windows client, because its records declare every wire name
# and its type explicitly with `JsonPropertyName` — the only place in this
# codebase where the whole surface is stated in one file.
#
# Ratcheted: this list may shrink freely and grows only by raising the
# ceiling in the same commit — a deliberate, visible edit, the same bargain
# as the doorless files. Fix one by giving each meaning its own name, the
# way `spoken` became `spoken_steps`, `deepened_line` and `spoken`.
#
# The four recorded at 0.61.1+: `attached`, `goal`, `intimate` and `sealed`
# surfaced when the phones gained the care-team, capture and channel-2
# doors the console has had since task #106. They are backend facts the
# console has been living with untyped — the capture listing's `intimate`
# is a boolean and the vocabulary's is a list — and renaming a served wire
# field is an API break that belongs to its own round, not to the round
# that finally declared the types.
#
# `step` and `steps` joined when the guide reached the phones: the
# tutorial's progress carries a lesson object and a chaptered lesson list,
# while the first-aid playbook has carried plain instruction strings under
# the same names since 0.15.x.
#
# `faces` joined when the dock reached the phones: the vocabulary's
# name-to-description table and the per-user settings' chosen-names list
# share the name, both served that way since 0.19.x.
#
# `medications` joined the same way when the cabinet reached the phones:
# the board's rows and the adherence tally's rows are different shapes
# under the same name, and both routes have served them that way since
# 0.9.0.
#
#
# ## 1.8.5 — the five primitive clashes close
#
# The dangerous class went first: a decoder coerces a primitive and hands
# the reader a plausible wrong answer. Each meaning has its own name now:
#
#   answered   the engaged act's HTTP-shaped code is `status_code`; the
#              adaptation tally's total is `answered_count`; the follow-up's
#              yes keeps the word.
#   helped     the tally and the finetune artifact count as `helped_count`;
#              the finetune listing serves the yes/no as the bool it is;
#              the follow-up's yes keeps the word.
#   chosen     the surfaces' top-level string is `chosen_surface`; each
#              row's boolean keeps the word.
#   goal       the savings number is `goal_amount`; the care plan's and the
#              handoff's sentence keep the word. The column stays `goal` —
#              the ambiguity was never in the database.
#   value      the boundary's boolean is `stands` (`holds` was itself
#              taken by a permission's string — the file's own lesson); the baseline metric's
#              number keeps the word.
#
#
# ## after 1.8.9 — ten of the fifteen object-shape rows close
#
#   attached    the mic channel's boolean keeps the word. The Windows and
#               iOS attach-outcome fields were PHANTOMS — the wire has
#               always served `release`, and both shells decoded a field
#               that never arrived. The records now say `release`.
#   cue         the cue-name and coaching-line strings keep the word; the
#               CPR pace object is `pace_cue`.
#   days        the meal plan's day list keeps the word; the adherence
#               window is `window_days`.
#   insights    the insight rows keep the word; the progress report's
#               tally is `insights_count`.
#   first_aid   the playbook keeps the word; the robot's rating string is
#               `first_aid_rating`.
#   intimate    the capture row's boolean keeps the word; the vocabulary's
#               site list is `intimate_sites`.
#   messages    the thread's message rows keep the word; the thread list's
#               tally is `messages_count`; the alarm's texts are
#               `alert_texts`.
#   sealed      the custody seal keeps the word — it is PDI's field,
#               passed through, and not JIM's to rename. The capture-row
#               boolean was another phantom (nothing serves it; a seal
#               badge would have read always-unsealed) — struck from the
#               three shell records rather than renamed.
#   specialist  the guidance label string keeps the word; the money
#               screen's door is `specialist_door`; the coach answer's
#               person is `specialist_who`.
#   step        the escalation flow's id keeps the word; the tutorial's
#               object is `next_lesson` (`lesson` was already the coach
#               store's text — this file's own lesson, learned twice), its chapter list `lessons`, and the
#               outline's tally `lessons_count` — the words the tutorial's
#               own database column already used.
#
# ## after 1.8.9, the second round — the last five rows close, and the
# ## file reaches zero
#
#   faces        the vocabulary's name-to-description table keeps the
#                word; the settings' chosen list is `chosen_faces`.
#   medications  the cabinet board's rows keep the word; the adherence
#                tally's rows are `adherence_rows`.
#   provenance   guidance's account (and the coach's superset of it, and
#                the capture row's code) keep the word; the engaged
#                turn's who-generated block is `generation`; the
#                specialist answer's method-and-shared is
#                `answer_provenance`; the capture vocabulary's code
#                table is `provenance_kinds`.
#   providers    the LLM roster keeps the word; the app catalog's
#                companies are `app_providers`; the sign-in doors are
#                `signin_providers`.
#   steps        the instruction strings (playbook, watch setup) keep
#                the word; the calm protocol's say-and-seconds rows are
#                `calm_steps`.
#
# Zero rows, ceiling zero: from here a new collision is a build error,
# not a backlog entry. The guard still runs on every commit — this file
# staying empty is the feature.
