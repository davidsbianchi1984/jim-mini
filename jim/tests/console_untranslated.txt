# status: floor — 2 rows
#
# ## 1.8.5: the last two rows are names, and names are the floor
#
# *ElevenLabs* and *OpenAI* are the two providers the voice-key picker
# names. A brand names itself in every language — translating one would
# manufacture a company that does not exist, the same reasoning the
# language picker uses for "English" and the tandem badge uses for
# "LIVE · QRME". They stay, and they are the whole of what stays.
#
# ## 0.69.1: the reader learned to see a sentence chosen at render time
#
# A field report on PDI — "It seems to be blocking the PDI menus" — began
# with a pill sitting on a tab bar and ended here. The pill's own words,
# *vault answering*, were English on a console that translates everything
# else, and the extractor behind every count in this estate reported the
# file clean: `{ok ? "vault answering" : "vault degraded"}` is a
# `ConditionalExpression`, not a `JsxText` node, so a sentence chosen at
# render time was text to the reader and nothing at all to the reader of
# the reader.
#
#     asked     what text does this file place between its tags
#     mattered  what words does this screen put on the glass
#
# The extractor now reads string literals in child position — both branches
# of a ternary, either side of `&&`, the pieces of a concatenation — and
# skips anything with no letter in it, because a spacer is drawn but is not
# a word. It still refuses call arguments, so `t("key", lang)` stays a key.
#
# 93 rows below are what that widening found. They are not new English;
# they are English this file has been unable to see for as long as it has
# existed, and the honest thing is the number rather than the floor it was
# resting on. The direction of travel is unchanged — the rows come off one
# screen at a time — but the count they come off is the true one.
## English on the JIM console. Checked by
# test_the_console_speaks_one_language.py, both directions, and ratcheted.
#
# **A floor as of 0.45.1.** Every screen of this console — pre-session and
# gated alike — is localized into all ten languages. The file stays, with
# its ceiling at zero, because the guard reads it in both directions: a
# single new English string on any screen fails the build, and the only way
# to add one is to write the row down here and raise the ceiling on purpose.
#
# What it used to say, kept for the record: "The rest of the console is not
# — 700-odd strings across nineteen gated screens, recorded here so the
# remainder is a decision somebody wrote down rather than a surface nobody
# looked at."
#
# **Corrected in 0.30.10.** Sixty-two of the rows here were separators, not
# English: a bare `:`, a `·`, a `%`, a `⚠` — fragments left between two
# interpolations, carrying no word anybody reads. The extractor counted any
# string it found; the guard then fired on a card whose every sentence had
# just been localized.
#
#     asked     did the extractor find a string here
#     mattered  did it find a word a reader reads
#
# The same mistake the shells' version of this guard made with
# `"\(dim): \(n)%"`, one file over and one release later.
# ceiling: 2
#
# The ceiling was 0 and went to 93: the reader widened, not the console.
# Every row it gained was already on the glass. Raising a ceiling is the one
# move this estate treats as a claim rather than a chore, so it was written
# here beside the reason, with a promise that the next rounds would take it
# back down.
#
# **0.75.0.** They did. Ninety-one of the ninety-three now have keys and ten
# translations each, across Attending, Baseline, Bearing, CareTeam, Channel,
# Checkin, Community, Held, Journal, Meds, Monitor, Reach, Safety, Wards,
# Coach, Onboarding and Settings.
#
# Three of the ninety-one were not translation work, and they are the ones
# worth reading the diff for. Onboarding's password toggle carried its
# `aria-label` in English beside a visible label that was also English, so
# the screen reader and the eye were untranslated in the same breath — a row
# this file could never have listed, because an aria-label is not text
# between tags. The Settings posture card built one sentence out of two
# adjacent string literals, which is a sentence no translator can reorder;
# it is one key now. And `OfflinePosture` is its own component with no
# language binding at all, because until this round it had nothing to say.
#
# Seven of the new keys collided with keys already in the table. Three were
# the same English and were dropped — the screens name the rows that were
# already there. Four were a *different* sentence under the same name:
# `set.voice.device` is a paragraph about the device voice, `med.add` is a
# heading, `med.critical` is a sentence, `hld.src.allow` takes a `{source}`
# hole. Those got a distinct name for the new use, and only the new call
# site moved. Two meanings under one key is this console's version of the
# defect `test_one_name_one_type_on_the_wire.py` catches on the wire.
#
# ## Why two rows stay
#
# `ElevenLabs` and `OpenAI` are the names of two companies. They are printed
# because that is what the provider is called, in every language, and a
# translated vendor name would be a name nobody could match against the
# account they hold there. They are quoted the way `TheMark: AI ·` is quoted
# one repo over: English on purpose, and recorded as such rather than left
# to look like an oversight. A floor of zero would have been a nicer number
# and a less honest one.
Settings: ElevenLabs
Settings: OpenAI
