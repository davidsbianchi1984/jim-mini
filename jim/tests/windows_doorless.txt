# status: backlog — 31 rows
#
# Every route this shell cannot reach.
#
# ## 0.71.0: engaged sessions landed on all four clients at once
#
# `jim/engaged.py` is the online Guardian a person leaves running: open until
# they sign off, acting across their own records through a written allowlist,
# and recording the request that would take each act back.
#
# It arrived here in the same round as the console rather than a round later,
# and the reason is the feature's own bargain. Full-act autonomy is defensible
# *because* every act is listed with the way back beside it. A shell that could
# open a session and speak into it, but could not show the undo trail, would
# have taken the permission and dropped the condition it was granted under.
# All fifteen or none.
#
# Six of the fifteen are `DELETE` routes that are also doors somebody should
# always have had: until this round nothing in this product could delete a
# journal entry, a check-in, a goal or a habit. Those are worth having on a
# phone on their own account, and they landed with the screen that made them
# necessary.

# ## 0.72.0: the Studio is console-first, and this is the debt that says so
#
# `jim/widgets.py` gives somebody a box to run their own code in — no network,
# one directory, no other programs, finite time — and the seven routes below
# are its doors. They landed on the console this round and not here, which is
# a decision rather than an oversight, and the decision is recorded rather
# than left implicit because "held at zero" was a real posture and this file
# is no longer at zero.
#
# The reason is the shape of the thing. A widget is written, not tapped: it is
# a name, a body of JavaScript, and a JSON input, edited over several passes.
# A phone keyboard is the wrong instrument for the first draft of a program,
# and shipping a cramped editor on three shells to make a number stay at zero
# would have been building for the record rather than for the person.
#
#     asked     can every client reach every route
#     mattered  is each client the right place for this one
#
# What that argument does *not* excuse is the reading half. Somebody who wrote
# a widget at a desk should be able to open it, run it and read the answer
# from the phone in their pocket, and today they cannot. That is the next
# round's work, and these rows are what will still be here saying so.

#
# ## 0.92.0: the far end landed on the console first
#
# `jim/farend.py` — the escalation ladder's `notify_contact` rung became a
# real letter to a consented address, with an acknowledgment link and a
# monthly liveness note. The console's Held screen gained the card that
# sets the address and shows whether a person saw the last alert; these
# two rows are that same card not yet carried to this shell. The ack link
# itself needs no row anywhere: it opens in the emergency contact's own
# browser — a person with no account who installs nothing.

#
# ## after 1.8.9 — the nine doors open
#
# (Written as the 1.8.5 round on a branch; a parallel line released
# 1.8.5–1.8.9 first, so the nine closes land folded onto that line.)
#
# The Studio's seven widget routes and the far end's two, on this shell:
# a Widgets card (list, write, show, run, remove, and the limits line) and
# a Far end card (the address or the honest refusal, save-with-consent,
# clear). The console-first debt the 0.72.0 note named is paid on all
# three shells in one round, the same all-or-none bargain the engaged
# sessions round set.
#

# ## after 1.8.9, second close — the staleness trio opens, and the file
# ## returns to zero
#
# The heartbeat, the freshness card and the meeting-recording door landed
# on this shell together: one beat sent on the day screen's way in, the
# two ages and the verdict beside the channel that produces them, and a
# running stretch takes an audio file straight to the transcriber. The
# 1.8.8 note above said the shells owed their own tellings; paid.

# --- the hands (2.7.0 / jim/hands.py) ---------------------------------------
#
# Fifteen routes, and this shell reaches none of them. The reason is one
# thing, and it is not scheduling.
#
# A hand needs a motor: something that can actually move a pointer, press a
# control and type into a field on the surface in front of it. None of these
# three shells has one, and what it would take differs per platform:
#
#   a motor here is a desktop automation service running beside the shell.
#   Until it exists, binding these fifteen calls would take them off this
#   list and build nothing.
#
#     asked     can this client reach the route
#     mattered  can this client do the thing the route records
#
# Binding the fifteen calls now would be the precise failure the unused-
# binding guard exists to catch. The web console has the whole surface
# today, and the thing that can photograph a screen and then act on it is
# the companion program that runs on the person's own desk. These rows
# stand until a motor does.
DELETE /profiles/{profile_id}/hands/grants/{grant_id}
GET /hands/vocabulary
GET /profiles/{profile_id}/hands/grants
GET /profiles/{profile_id}/hands/reaches/{reach_id}
GET /profiles/{profile_id}/hands/routines
POST /profiles/{profile_id}/hands/grants
POST /profiles/{profile_id}/hands/reaches
POST /profiles/{profile_id}/hands/reaches/{reach_id}/act
POST /profiles/{profile_id}/hands/reaches/{reach_id}/hand-over
POST /profiles/{profile_id}/hands/reaches/{reach_id}/next
POST /profiles/{profile_id}/hands/reaches/{reach_id}/stop
POST /profiles/{profile_id}/hands/routines
POST /profiles/{profile_id}/hands/routines/from-reach
POST /profiles/{profile_id}/hands/routines/{routine_id}/replay
POST /profiles/{profile_id}/hands/told
# ## 3.0.2: the reach-out operator screen is a desktop affordance
#
# The reach-out cascade — JIM ringing the emergency contacts one after
# another — fires automatically on a crash-watch trip, on the backend, no
# matter which surface armed the watch. What this round added a door for is
# the *operator*'s view of it: reading the dialer's held-shut posture,
# listing the cascades that are running, and starting one by hand. That is a
# console screen (app/src/screens/Safety.tsx). The phone's crisis job is the
# one it already must do and does — answering the alarm, which is what stops
# the ladder — and the automatic trigger needs no phone door. Starting and
# watching a cascade by hand is deferred here on purpose.
GET /dialer/{user_id}/posture
GET /reachout/{user_id}
POST /reachout/{user_id}
# ## 3.0.3: the moderated mailbox is a desktop operator surface
#
# The coach agent's correspondence — read an inbound message, have the agent
# draft a reply, approve/edit/discard it, or write to someone new — is a
# console screen (app/src/screens/Mail.tsx). Every outbound message is held
# for a person to approve; the moderation queue is an operator affordance, not
# a crisis surface. The phones defer it on purpose this round; when a phone
# grows a mailbox these come with it.
GET /mail/{user_id}
GET /mail/{user_id}/posture
POST /mail/{user_id}/compose
POST /mail/{user_id}/draft/{draft_id}/moderate
POST /mail/{user_id}/message/{message_id}/draft
POST /mail/{user_id}/receive
# ## 3.0.4: the offline training corpus is a desktop settings surface
#
# Every exchange is banked as training data at the backend choke point
# (jim.llm.generate_for_user) no matter which client made the request — that
# capture needs no phone door. What this round added a door for is the
# person's *view and control* of it: the bank's size, the capture switch,
# archive-to-vault, and the purge. That is a card on the console's Settings
# screen (app/src/screens/Settings.tsx). The phones defer the control surface
# this round; capture reaches the corpus from them already.
DELETE /corpus/{user_id}
GET /corpus/{user_id}
POST /corpus/{user_id}/archive
PUT /corpus/{user_id}/consent

# ## 3.0.5: the model menu by region, and app edits — desktop surfaces
#
# The region is chosen at sign-up on every client (it is an Enroll field), and
# the backend applies the loadout no matter which client asks — so the
# *menu* reaches the phones already through the model picker's own doors. What
# this round added desktop doors for is reading the loadout with its origins,
# changing region afterwards, the video menu (QRME's), and the app-edit seam:
# proposing a change, having the assistant draft one, and reading your own.
# Those are Settings and Studio surfaces on the console; the phones defer them
# this round. Company oversight's doors are reviewer-token, not client, doors.
GET /models/{user_id}
GET /video/providers/{user_id}
PUT /users/{user_id}/region
